{
  "name": "devseed-stories",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "yarn@4.13.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @devseed-stories/editor dev",
    "build": "yarn workspace @devseed-stories/editor build && yarn workspace @devseed-stories/viewer-app build",
    "build:publication": "yarn workspace @devseed-stories/publisher build:fixture",
    "check:independence": "node scripts/check-independence.mjs",
    "test": "vitest run",
    "typecheck": "tsc --build",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
