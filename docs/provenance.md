# Provenance

DevSeed Stories began as a product extraction from the MIT-licensed CNG Sandbox
prototype created by Anthony Boyd. The initial repository selectively adapts
story schema, viewer, map-rendering, and export concepts proven there.

The repositories are separate products. DevSeed Stories does not consume CNG
Sandbox as an upstream dependency, and there is no expectation that changes
will be synchronized after the initial extraction period.
