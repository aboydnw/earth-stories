{
  "name": "@earth-stories/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc --build",
    "dev": "yarn workspace @earth-stories/local-service build && yarn workspace @earth-stories/viewer-app build && yarn workspace @earth-stories/editor build && tsc --build && electron dist/main.js --development",
    "test": "vitest run src"
  },
  "dependencies": {
    "@earth-stories/local-service": "workspace:*"
  },
  "devDependencies": {
    "electron": "43.4.0",
    "typescript": "^5.9.2"
  }
}
